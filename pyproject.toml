[project]
name = "django-prack"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Django==6.1.1",
    "dj-database-url>=2.3,<3.0",
    "psycopg[binary]>=3.2,<4.0",
]

[tool.vercel.scripts]
build = "python manage.py migrate --noinput && python manage.py ensure_admin"
